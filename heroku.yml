setup:
  addons:
    - plan: heroku-postgresql  # Add Postgres database

build:
  docker:
    web: Dockerfile             # Build web image using Dockerfile

release:
  command: python manage.py collectstatic --noinput  # Collect static files

run:
  web: gunicorn django_project.wsgi:application     # Start Gunicorn with WSGI
